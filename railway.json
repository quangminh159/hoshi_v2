{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash -c 'python -m pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput'"
  },
  "deploy": {
    "startCommand": "gunicorn hoshi.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 